Find Minimum and Maximum in a matrix

The main functionality is provided by function find_min_max 
which can take any matrix as input (2D array), num of rows and num of columns
And prints the min/max numbers with their index, row, column
the main number of columns is specified by the macro NUM_OR_COLUMNS
if you want to change the dimensions of the parameter matrix you must change the NUM_OR_COLUMNS